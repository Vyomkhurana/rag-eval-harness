# Copy to .env and fill in ONE of these. get_llm() picks the first that is set,
# in this order: Anthropic, OpenAI, Gemini. If none is set it falls back to MockLLM.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Google AI Studio key (free tier). GOOGLE_API_KEY also accepted.
GEMINI_API_KEY=
